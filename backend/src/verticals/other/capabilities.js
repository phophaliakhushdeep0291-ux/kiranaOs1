/** Features enabled for a Other / custom business shop.
 *
 * Deliberately minimal. `settingsForBusinessType` lets a custom shop switch on any
 * capability in the platform vocabulary, so the preset is a starting point, not a ceiling. */
export const capabilities = [
  "BASIC_INVENTORY",
];

export default capabilities;
