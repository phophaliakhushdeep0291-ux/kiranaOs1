/** Runtime equivalents of the backend's shared role/status types. */
export const USER_ROLES = Object.freeze(["owner", "admin", "manager", "staff"]);
export const SETUP_STATUSES = Object.freeze(["pending", "complete"]);
export const PAYMENT_MODES = Object.freeze(["cash", "upi", "card", "bank", "credit", "split"]);
