node_modules/
frontend/node_modules/
backend/node_modules/

.env
.env.*
!.env.example

dist/
build/
coverage/

*.db
*.db.corrupt
*.sqlite
*.sqlite3
dev.db

*.log
*.orig
*.rej
*.tmp

# Local archives / handoff bundles should not be committed.
*.zip

.vscode/
.idea/
.claude/
.DS_Store

# `vercel link` writes the linked project and organization ids here. It is local
# linkage to a Vercel project rather than product source, and it lands untracked
# in whichever directory the CLI was run from — the repository root or frontend/,
# which is this project's Vercel Root Directory — where a `git add -A` sweeps it
# into an unrelated commit. Unanchored so it matches at either depth.
.vercel/

# One-off local probes and deployment-trigger marker files are never product
# source. Keep their reusable scripts under scripts/ instead.
__qa_*
patch-test.txt
*-redeploy.txt

# Generated browser/test evidence belongs in CI artifacts, not Git history.
qa-artifacts/
test-results/
playwright-report/

# Accidental mounted workspace paths and runtime shop data.
/backend/prisma/mnt/
/backend/storage/exports/
/backend/storage/backups/

# Windows installer staging is reproducible from the signed release workflow.
/hardware-bridge/windows/stage/

# Generated UI verification screenshots (runtime assets live under frontend/public).
/frontend/*.png

# Standalone side project kept alongside the POS, with its own node_modules. It
# is not part of the app build, and leaving it untracked put a few thousand
# files in every `git status`.
/hotel-demo/
/hotel-pitch/

# Release-certification output. These are run artifacts pinned to one commit —
# `ci27/` and `ci29/` were committed by accident and are two failed runs from
# 2026-07-13 that nothing reads.
release-certification-*.json
release-certification-*.md
/ci[0-9]*/
# Short, isolated release snapshots avoid deeply nested pnpm paths on Windows.
/.release-qa/
# Unrelated prototypes retained outside the product build/test/lint/CI surface.
archive/

# Throwaway Chrome profiles from the QA capture scripts.
#
# Those scripts default their --user-data-dir to a temp directory, but they honour
# QA_PROFILE_DIR / QA_OFFLINE_PROFILE_DIR, and a run given a RELATIVE name like
# "-offline-all-routes-final" resolves it inside frontend/. Chrome then writes its
# whole profile there — caches, cookie databases, crash dumps, service-worker
# storage. Five such runs had left 151 MB across 2,600 files staged for commit and
# waiting to be swept into the next one; nothing reads them and they are recreated
# on every run. A leading dash is not a name any real source file here carries.
/frontend/-*/
