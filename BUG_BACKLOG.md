# Bug Backlog

Status: Active  
Last triage: 2026-08-11

## Severity and workflow

- P0: data loss/duplication, wrong money/stock/ledger, cross-tenant/security breach, or core billing unavailable. Blocks all releases and feature work.
- P1: core flow materially broken or unreachable with no safe workaround. Blocks the affected release.
- P2: degraded behavior with a safe workaround.
- P3: polish or low-impact cleanup.

States: `New -> Reproduced -> In progress -> Fixed -> Verified -> Closed`; use `Blocked` only with an owner and next action. Never delete a closed bug; link the fixing commit and regression test.

## Active backlog

| ID | Sev | State | Requirement | Summary | Evidence / next action |
|---|---|---|---|---|---|
| BUG-001 | P0 | Verified | BILL-002, BILL-008 | Frontend billing coupon field typecheck regression reported in the Phase 1 brief. | Frontend typecheck passed and `coupon-atomic-billing.test.ts` passed 3/3 on 2026-07-16; full frontend suite passed 611 tests. Close when the fixing commit is published. |
| BUG-002 | P1 | Verified | RPT-003 | Reporting authority is explicit: operational tables plus locked daily snapshots remain customer-facing truth; `FinancialLedger` is an append-only journal and future read-model candidate. | Owner/admin-only all-time reconciliation exposes exact paise variance and blocks read cutover unless all supported KPIs match. Focused integration proves staff denial, zero parity, intentional 1-paise drift detection, and cancellation netting (11/11 on 2026-07-31). See `CODE_REVIEW_LOGIC_FLAWS.md` item 4. |
| BUG-003 | P2 | New | BILL-004, RPT-002 | Exclusive-GST discount tax-base behavior needs a compliance decision. | Obtain accountant/product ruling, document tax policy, then add fixture parity test. See logic review item 6. |
| BUG-004 | P3 | Verified | SYNC-002 | Offline identity resolution no longer assumes every UUID is client-local. | Client-created records already use explicit `local_`/entity prefixes, so the over-broad UUID heuristic was removed. CUID, UUIDv4, UUIDv7 and ULID server identities now remain eligible for normal lookup/validation instead of being stranded as `SYNC_DEPENDENCY_PENDING`; focused ID-format examples pass on 2026-08-08. |
| BUG-005 | P1 | In progress | BILL-007, HW-001 | The one-click signed-installer source, automatic service, native printer picker/test print, six-character ten-minute single-use pairing, credential rotation, update notice, durable restart journal and simulated failure/retry guards are implemented. Print jobs are bound to a SHA-256 payload fingerprint across concurrent calls and restarts; legacy unbound rows fail closed. Installer upgrades stop/refresh/restart the service, remove inherited ProgramData credential ACLs, require package/health version parity and hash-pin WinSW before signing. `hardware-bridge` passes 15/15 software tests and the release PowerShell parses cleanly. | Certification is explicitly limited to **TVS RP 3160 Gold** and **Epson TM-T82III**; every other model is unsupported. BUG-005 remains open for both models because a signed-installer workflow run, clean-Windows install and physical normal/paper-out/disconnect/power-off/retry/no-duplicate/drawer artifacts have not been produced in this workspace. Close only the individual model rows after all retained evidence in `hardware-bridge/README.md` is complete; do not treat software simulation as physical certification. The native setup app was not rebuilt locally because this workstation lacks the .NET SDK. |
| BUG-006 | P1 | Verified | INV-006 | Barcode generation/printing and reorder suggestion coverage is established. | Products can generate checksum-valid restricted-circulation EAN-13 codes, print one or all selected price labels with EAN-13/QR fallback, and report blocked print popups. Purchase orders expose deterministic 30-day demand/safety-stock/open-order evidence, supplier-separated editable suggestions, receipt reconciliation, and stock/cost updates. Frontend barcode/label and reorder-transparency coverage passed 15/15 and backend reorder examples passed on 2026-08-08; physical printer certification remains tracked separately by BUG-005. |
| BUG-007 | P1 | Verified | INV-004 | Supplier ledger end-to-end reconciliation evidence is complete. | PO receipt proves partial paid/due allocation, exact stock, weighted-average cost, retry safety and supplier-history uniqueness. Focused 390px settlement/reversal QA proves ₹900 -> ₹700 -> ₹900, owner PIN, immutable history, exact-event replay, 44px controls and zero overflow/runtime errors. |
| BUG-008 | P1 | Verified | SYNC-003 | Conflict handling is certified by entity rather than using one unsafe last-write-wins rule. | Product, customer and supplier snapshots allow owner-audited local/cloud resolution while excluding stock, cost and ledger-derived balances. Bill, payment, Udhar, stock-ledger and purchase conflicts are forced through reversal/compensating-entry workflows and remain open after a rejected overwrite attempt. Event classification now preserves those financial entity types (including `UDHAR_PAYMENT`, which previously looked like a mutable customer conflict). The classifier matrix passes, focused frontend conflict/redaction coverage passes 13/13, and the forced API sync integration passes 45/45 on 2026-08-08. |
| BUG-009 | P1 | In progress | QUAL-003 | Full live mobile screenshot matrix is not automated or retained for all core pages. | The repeatable CDP harness retains 32/32 passing initial-route captures for Billing, Products, Customers, Inventory, Purchase Bills, Reports, Settings and Sync Status at 375x667, 390x844, 430x932 and 768x1024. A separate production-service-worker gate closes Chrome, restarts it with uncached backend probes blocked, and retains 11/11 passing 390x844 cold-restart captures for Dashboard, Billing, Products, Customers, Inventory, Bills, Purchase Bills, Reports, Settings, Sync Status and Recovery Mode; product/customer data restored in 1.53s/2.24s and every route stayed under the 10s ceiling with zero bounce, overflow, stale scroll, fatal/loading/runtime error. Keep open until every remaining stateful/keyboard QA flow in `MOBILE_UX_PLAN.md` has equivalent retained evidence. **2026-08-08: the matrix now also covers `/udhar` (MQA-UDHAR-01), so the required set is 36 captures, not 32, and the retained 32 predate it.** The udhar screen previously claimed four widths through an `it.each([375, 390, 430, 768])` in `udhar-home.test.ts` that never read the width and re-ran one source-string assertion four times; that test now states only what it checks, and the viewport proof belongs to this harness. |
| BUG-010 | P0 | Verified | SYNC-005, QUAL-004 | Railway container crashed because `src/modules/backups/backup.service.js` was absent from the Git build. | Root cause: unanchored `backend/.gitignore` entry `backups` hid the source directory. Changed it to `/backups/`, made the service visible to Git, and added deployment/production-check regressions. Railway `/api/health`, `/health`, and `/health/ready` returned HTTP 200 on 2026-07-16; database and storage checks reported `ok`. |
| BUG-011 | P0 | Verified | INV-002, SYNC-002 | A purchase-receipt retry key could alias a changed payload or a different PO, and successful retries duplicated the owner audit event. | Replay compatibility now binds the key to PO, payment, invoice, item quantities/rates and batch dates; mismatch returns `IDEMPOTENCY_KEY_REUSED`. Controller emits audit/webhook only for the first mutation. The branch-aware purchase integration proof passed on 2026-07-18. |
| BUG-012 | P0 | Verified | BILL-006, INV-002, SYNC-002 | An offline owner cancellation reversed local Udhar but left the sale's stock deducted until a later server pull; a repeated local action could append the cancellation correction twice. | Cancellation is now one local transaction across the bill, pooled/per-pack stock, inventory reversal, Udhar correction, audit and stable outbox event, with stable correction identities and an already-cancelled guard. Snake-case server-hydrated Udhar bills are normalized before reversal. Backend billing passes 19/19; the focused cancellation/reconciliation/security set passes 29/29. Live 390px bill #2 remained Cancelled after reload, restored Mohan's Udhar ₹362 -> ₹356, retained the owner reason, removed destructive actions, used 44px controls, and had zero overflow/console errors. |
| BUG-013 | P0 | Verified | SYNC-003, BILL-008 | A rejected owner action could expose the entered owner PIN inside the sync-conflict field diff and technical details. | Conflict snapshots are recursively sanitized before local persistence, cross-device reporting and display; credential keys are omitted while `ownerPinProvided` evidence remains. Focused redaction coverage passes and live Sync Status contains neither the PIN value nor the owner-PIN field. |
| BUG-015 | P1 | Verified | ADM-001, SYNC-002 | Three simultaneous logins on a 2-slot plan could time out one SQLite interactive transaction and kill Prisma's query engine. | SQLite now uses one connection, serializes the per-shop decision, and commits the device plus session as one non-interactive batch; PostgreSQL retains its cross-process advisory transaction lock. The clean 2026-08-10 certification passes all 14 device-session cases, including the three-login race in 875 ms, with exactly two registrations, one `DEVICE_LIMIT_EXCEEDED`, and a healthy client for every later integration file. |
| BUG-014 | P1 | Verified | QUAL-003 | The shell class refactor left desktop `<aside>`/`<header>` elements without mobile fallback rules, allowing desktop and phone navigation chrome to render together after session unlock. | Explicit max-1023px shell geometry hides desktop chrome and removes desktop margin. Live 390x844 post-unlock proof reports both desktop elements as `display:none`, stable Billing History title, mobile bottom navigation, 390px document width and zero runtime errors. |
| BUG-016 | P0 | Verified | SYNC-005 | Restaurant add-on menus and sold-line choices could be omitted from a complete restore. | `MenuAddonGroup`, `MenuAddonOption`, and `ProductAddonGroup` are classified as restorable shop configuration; `BillItemAddon` restores through `BillItem` as immutable receipt history. The exact backup schema is bumped to `2026-08-08-complete-v3`, and the model-policy plus transactional backup tests guard the classification. |
| BUG-017 | P0 | Verified | SYNC-002, SYNC-003 | A stale offline customer edit could silently overwrite a newer server edit, and one rejected operation could appear as two owner-review conflicts. | Customer updates now carry and exactly compare the synced `baseUpdatedAt`; mismatches retain one authoritative server conflict, reuse that row for legacy client reports, collapse historical duplicates in the UI, cascade linked resolution, preserve ISO timestamps in snapshots and write an owner/device audit. Focused frontend coverage passes 33/33 and the targeted backend two-device integration passes 1/1. Live 390x844 QA showed Device B's stale name beside Device A's cloud name in one customer review card; the authenticated `use_server` decision resolved the only open customer row, produced `SYNC_CONFLICT_RESOLVED`, and left the customer ledger unchanged. |
| BUG-018 | P0 | Verified | SYNC-005, QUAL-004 | Rolling back a restore with its automatic recovery artifact restored business data but erased the audit row for the forward restore. | The serializable restore transaction now carries backup/restore control-plane audit rows forward while rewinding the historical business audit from the artifact. The focused recovery suite passes 4/4: state B returns exactly, both restore actions remain auditable and `dataEpoch` advances twice. The broader encrypted-backup suite passes 8/8. |
| BUG-019 | P0 | Verified | BILL-002, BILL-003, INV-002, QUAL-002 | Restaurant portions were treated like retail packs when applying the product MRP ceiling. A Large portion priced at ₹590 with a 1.4 recipe conversion was silently clamped to ₹588, so a ₹85 add-on persisted as the wrong ₹673 total instead of ₹675. | Portion units now use only an explicitly configured portion maximum; retail pack/batch ceilings remain unchanged. Frontend and backend regressions pass, and the database integration persists ₹590 + ₹85 = ₹675 while consuming exactly 1.4 dish units, 0.56 recipe units and 0.1 add-on ingredient units. Live 390x844 checkout and bill history both showed ₹675. |
| BUG-020 | P1 | Verified | BILL-007, QUAL-003, QUAL-005 | Restaurant variation and add-on snapshots were not presented clearly in final bill detail/duplicate receipts, and the first phone bill-detail implementation put the item table inside an internally scrolling 346px region. | Bill detail and duplicate-print data now normalize immutable add-on snapshots across supported field shapes. Phones use a full-width premium item card while desktop retains the table. Live 390x844 proof shows Large, `Finish your plate: Smoked mozzarella +₹85`, Qty/Rate/Total, document 390/390 and item card 346/346 with no horizontal scroll; focused bill-option, receipt and mobile-layout tests pass. |
| BUG-021 | P1 | Verified | BILL-002, BILL-003, QUAL-003, QUAL-005 | The 375px checkout audit exposed portion/rate/discount/note, quantity, remove, coupon/loyalty, partial-payment, cash-tender and clear-cart controls below 44px. Tablet add-on options exposed a 42px clickable child and the shared dialog close affordance shrank to 32px at `sm`. | Billing cart rows now become a two-row phone card with 44px controls and retain a compact five-column tablet/desktop layout. Checkout disclosures/tender controls are at least 44px; restaurant option buttons and the shared dialog close target remain 44px at every breakpoint. Live settled-state QA passes 375x667, 390x844, 430x932 and 768x1024 with zero document/internal overflow and zero measured sub-44px controls. Focused regressions pass 12/12 and the current full frontend suite passes 1,335 with one skip. |
| BUG-022 | P1 | Verified | QUAL-002, QUAL-004 | The credential brute-force limiter covered every `/api/auth/*` request, so ordinary `/auth/me` session checks consumed the same anonymous-IP bucket as logins and eventually returned `AUTH_RATE_LIMITED` during navigation/recovery. | The limiter now skips authenticated read/management routes and remains enabled for registration, login, OAuth, email/password, refresh, device replacement, PIN verification and password change. Scope/security examples pass and the production cold-restart matrix reuses the same session across all routes without auth-limit churn. |
| BUG-023 | P1 | Verified | SYNC-005, QUAL-004 | Recovery Mode's local integrity check queried a nonexistent IndexedDB `inventory` store, falsely reporting healthy devices as broken; the Recovery route was also absent from the mandatory offline app-shell cache. | Health checks now read active-shop rows through the scoped facade, validate stock from canonical product fields, include `inventory_movements`, and Recovery Mode is a critical precached route. The real production cold-restart gate opens Recovery offline in 1.29s with the backend blocked, service-worker control active and no local-database problem, bounce, overflow, stale scroll or runtime error. |
| BUG-024 | P1 | Verified | SYNC-005, QUAL-004 | The encrypted local-backup implementation passed runtime tests but failed the production TypeScript gate because decoded byte arrays were typed as `ArrayBufferLike`, which Web Crypto does not accept as a strict `BufferSource`. | Base64 decoding, SHA-256 and PBKDF2 inputs now retain exact `Uint8Array<ArrayBuffer>` types. The encrypted export/import, wrong-passphrase and tamper-refusal suite passes 3/3; the full frontend typecheck and current 1,335-test regression suite pass. |
| BUG-025 | P0 | Verified | SYNC-005 | Four committed shop-scoped models — `ChannelSettlementImport`, `ChannelSettlementRow`, `ChannelSettlementEvent` and `MenuComboComponent` — had no entry in `backend/src/modules/backups/backup-policy.js`, so a complete restore rebuilt the shop without its marketplace settlement records or its combo contents. | All four classified as `RESTORABLE_SHOP_MODELS`: each carries its own `shopId`, and each is the shop's own trade record rather than install state — settlement rows carry the commission, payment fee, TCS and TDS a return is filed on, the event rows are the append-only record of who resolved which mismatch, and a combo without its components is an empty thali. Row→Import and Event→Row are foreign keys, but `restoreModelOrder()` topologically sorts from the Prisma relation metadata, so list position is irrelevant (verified: StoreLocation 26 → Import 38 → Row 60 → Event 73). `tests/backup-model-policy.examples.js` now passes with 102 tenant models classified, and `npm run backup:drill` reconciles 1,781 records across 81 tables at 0 paise and 0 stock-unit variance with `npm run backup:drill:verify` still 8/8. |
| BUG-026 | P1 | Verified | QUAL-004 | `backend/prisma/dev.db` was stale relative to `prisma/schema.prisma`, so bill sync failed when combo expansion queried the missing `MenuComboComponent` table and shop cleanup could panic through missing cascade targets. | With the local backend stopped, `npm run prisma:push` synchronized the existing SQLite database to all 80 committed migrations/models without deleting its QA data. The previously failing `node tests/activity-personalization.examples.js` now completes and cleans up its shops successfully; the live ₹675 Udhar retry also reconciled after the schema sync. Production deployment remains governed by PostgreSQL migrations, not this local `db push`. |
| BUG-027 | P2 | Verified | SYNC-004, QUAL-005 | The 390px Sync Status audit found the icon-only `Refresh sync diagnostics` action at 36x36 even though the mobile contract requires 44x44. | `SyncDiagnosticsSection` now uses `h-11 w-11`, and `touch-target-primitives.test.ts` prevents regression. Post-fix in-app-browser QA at 390x844 measures the live refresh action at exactly 44x44 with document width 390/390 and zero horizontal overflow; `sync-status-refresh-44px-390x844.png` retains the UI state. |
| BUG-028 | P1 | Verified | SYNC-004, QUAL-004 | SQLite and PostgreSQL kept `ChangeLog.shopId` restrictive, so sync-trigger rows could block test/shop cleanup; delete triggers could also try to create a tombstone after the owning shop was already being deleted. | Both schemas and replay-safe migrations now cascade `ChangeLog` with the shop, while SQLite/PostgreSQL delete triggers emit tombstones only while that shop still exists. The isolated cascade example proves normal product tombstones remain intact and complete shop deletion leaves zero orphan change rows; the seven-case packaging/stock database suite passes. |
| BUG-029 | P1 | Verified | QUAL-004 | The historical SQLite migration folder is not a bootstrappable chain: its first `init` migration creates indexes on tables it never creates, so local `migrate dev/reset/deploy` paths fail on a clean database and can leave developers testing against stale schemas. | Normal SQLite schema commands now use a guarded, non-destructive `db push` without `--accept-data-loss`; explicit reset is restricted to `dev.db`/`test.db` outside production; generic deploy aliases route only to the maintained PostgreSQL chain. The updater was exercised against the guarded test DB, the live dev DB now has zero schema diff, both Prisma schemas validate, migration safety reports zero warnings, and production checks pass. |
| BUG-030 | P2 | Verified | QUAL-004 | A mutation-testing copy named `zz-mutant` was accidentally committed under production source together with its duplicate test. It was not imported by the app, but contained deliberately wrong product mapping and debug output and could be imported or packaged later. | Both mutation artifacts are removed. `production-check.js` now recursively rejects `zz-mutant*` and `*.mutant.*` files under `src` or `tests`, and the focused production contract plus the full production check pass. |
| BUG-031 | P1 | Verified | QUAL-003, QUAL-004 | A cold route import could leave Customers/Udhar on a long loading fallback, while the 12-second boot guard could show a fatal timeout just before a slow development start completed. | Billing, Customers and Udhar now share cached route loaders, warm during dashboard idle time and on pointer/focus/touch intent. The HTML shell renders an immediate neutral startup state and reserves the fatal timeout for 20 seconds while still surfacing real script/rejection failures immediately. Live desktop and 390x844 checks opened Customers and Udhar without the old fallback, retained the correct ₹354 balance and had no horizontal overflow; focused route/boot tests and the full production gate pass. |
| BUG-032 | P1 | Verified | QUAL-002, QUAL-005 | Customer statement printouts emitted translation calls such as `{t("customers.ledger.udharAmount")}` as literal text; generated customer/order/report HTML also inserted names, notes and product text without escaping. | Both Khata statement paths now interpolate the active language and every dynamic print cell passes through the shared HTML escaper. The same protection covers public QR-order slips and Daily Closing product rows, preventing print-window markup injection from customer-entered content. Focused coverage passes 17/17 and the exact-current production gate passes 1,350 tests, typecheck, translation parity, build, bundle budgets and production-app checks. |
| BUG-033 | P2 | Verified | QUAL-005 | The persistent mobile subscription banner rendered `Owner details` only 36px high, so every affected phone page retained an undersized global action even after its page-specific controls passed. | The shared banner and action now use a 44px minimum height. Focused regression coverage passes 9/9; live 390x844 Sync Status QA measures Owner details at 118x44, diagnostics refresh at 44x44, document width 390/390, zero horizontal overflow and zero remaining undersized active controls. |

| BUG-034 | P1 | Verified | ADM-001, QUAL-002, QUAL-004 | Staff administration could report locally cached/offline changes as if access had changed on the server; backend staff mutations did not consistently make the owner audit part of the same transaction, a password edit left existing staff sessions active, simultaneous invitations could pass the plan-seat check together, and disabled historical users still consumed paid seats. | Staff create/edit/deactivate is now server-authoritative and the offline cache is read-only. Create, update, role, disable and location-scope mutations require a durable audit inside their transaction; password edits revoke active sessions with `STAFF_PASSWORD_CHANGED`. Staff invitation performs feature, active-seat, duplicate, create and audit checks under a per-shop lock: SQLite/tests serialize in process and PostgreSQL uses an advisory transaction lock. The isolated owner/RBAC integration passes 18/18 on 2026-08-11: a forced audit-insert failure rolls the user creation back, exactly five concurrent Growth seats are admitted and the sixth is rejected, then one audited deactivation releases one replacement seat while historical identity remains; focused frontend server-authority coverage passes 3/3. |
| BUG-035 | P1 | Verified | ADM-001, QUAL-002, QUAL-003 | `Check payment status` refreshed the display subscription but not the signed device licence, so a confirmed Growth plan could remain locked behind an expired Starter licence; the no-row fallback also restarted its seven-day trial from the time of every request. | Refresh now fetches the current subscription and freshly signed device licence together, rejects unsigned or plan-mismatched responses before changing either cache, and persists the authoritative licence before the display snapshot. Missing-row trials are anchored to immutable shop creation time and repeated requests return the same trial end instead of extending access. Focused frontend coverage passes 8/8, subscription/device integration passes 11/11 and device-licence integration passes 6/6. Live 390x844 QA changed expired Starter to active Growth, unlocked Staff, then restored Starter gating after the temporary database row was removed. Settled Staff/Add Staff layouts also pass 375x667, 430x932 and 768x1024 without clipping or horizontal scroll; confirmation badges fit and active controls retain 44px targets. |
| BUG-036 | P2 | Verified | QUAL-003, QUAL-005 | The 375px Staff audit found both segmented tabs at only 28px high, and the long Add Staff task did not use the shared sticky mobile action footer. | Shared tab lists/triggers now have a 44px minimum target. Add Staff uses `DialogFooter`, producing full-width sticky safe-area actions on phones and compact modal actions at 768px. Settled live geometry at 375/430/768 reports zero sub-44px active controls and zero horizontal overflow. Focused mobile-task coverage passes, and the exact post-fix frontend production gate passes 1,374 tests plus typecheck/build/bundle/security checks. |
| BUG-037 | P1 | Verified | ADM-002, QUAL-003, QUAL-005 | The full mobile Settings audit found no direct Taxes & GST shortcut, 18-36px actions across printer/sync/security, shared 36x20 switch targets, a 24px toast dismiss action, an internally scrolling 720px security permissions table on phones and an unlabeled 15px password reveal action. | Taxes & GST is reachable directly from the phone hub. Shared switches and toast dismissal expose 44px targets, settings text actions retain 44px height, phone/tablet security permissions use fitted cards while desktop keeps the table, and the password reveal action is labeled and 44px. The settled 24-route matrix at 375/390/430/768 reports exact document/viewport width parity, zero overflow and zero active controls below 44px. Stateful Billing/Taxes/Printer/Sync/Security tasks passed without leaving mutations; evidence and checksums are in `docs/evidence/mobile-settings/manifest.json`. The exact frontend gate passes 1,384 tests plus typecheck, translation parity, build, bundle budgets and production-app checks. |
| BUG-038 | P1 | Verified | INV-001, ADM-001, QUAL-003, QUAL-005 | Live mobile Pricing QA found that a successful permanent rule creation remained invisible until a full reload because mutable pricing GETs could reuse browser cache. The same live flow showed owner audit attribution with a null device when an older session was not bound to the activated request terminal. | Pricing-rule, pricing-settings and selling-unit reads now use `cache: no-store`, so post-mutation invalidation renders the new/archived state immediately. Pricing audit actors prefer the device already verified by `requireDeviceActivated`, then fall back to session/header identity. The owner PIN clears after each successful permanent change; the Growth route gate remains enforced. Live 390x844 geometry reports 390/390 width, zero overflow and zero sub-44px active controls. Focused frontend coverage passes 11/11, atomic backend pricing coverage passes 1/1 including actor/device retention and forced-audit rollback, and the exact frontend production gate passes 1,393 tests with one intentional skip. Evidence and cleanup: `docs/evidence/mobile-pricing/manifest.json`. |

## Fixed findings awaiting historical closure

The dated resolution table in `CODE_REVIEW_LOGIC_FLAWS.md` reports fixes for report day counts/timezones, subscription grace, shop-header trust, webhook idempotency and udhar double-counting. Before closing them here, link each fixing commit and a passing regression-test run for the release candidate.

## Bug template

```text
ID: BUG-NNN
Severity / state:
Requirement IDs:
Environment and commit:
Preconditions:
Steps to reproduce:
Expected / actual:
Money, stock, ledger, sync or tenant impact:
Evidence (log/screenshot/correlation ID):
Root cause:
Fix commit:
Regression test:
QA flow and verifier:
```

## Triage rules

Triage daily during hardening and beta. A bug affecting money, inventory or offline writes must include backend validation and idempotency analysis. A mobile bug must name viewport and keyboard/nav state. Beta feedback becomes a bug only when reproducible; otherwise log it as a research observation and assign an experiment.
